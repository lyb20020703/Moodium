namespace VFXViewer
{
    // Legacy placeholder kept so externally generated project files that still reference
    // this path continue to compile. Active distance scaling logic now lives in
    // RecognitionZoneVisualAnchor.
    internal sealed class DistanceResponsiveVisualScale
    {
    }
}
